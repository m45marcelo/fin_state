import { ModalBackground } from "../ModalBackground"
import { CardModalEditSubscription } from "./CardModalEditSubscription"

export const ModalEditSubscription = () => {
    return(
        <ModalBackground modalName="editSubscription">
            <CardModalEditSubscription />
        </ModalBackground>
    )
}